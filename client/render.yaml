services:
  - type: static
    name: tfg-client
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - path: /*
        destination: /index.html
        action: rewrite